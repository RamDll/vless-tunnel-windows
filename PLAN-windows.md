# vless-tunnel для Windows — план реализации

Задача для агента. Сделать нативный клиент для Windows 10/11 по образцу
Linux-версии [RamDll/vless-tunnel](https://github.com/RamDll/vless-tunnel)
(эталон — v1.2.9). Переносится **поведение**, а не код: bash, iptables,
systemd и sudoers в Windows заменяются аналогами ниже.

Главное требование владельца: ссылки с post-quantum параметрами
(`encryption=mlkem768x25519plus…`, REALITY `pqv`) должны работать так же,
как на Linux и на роутере. Из-за них чужие клиенты не подходят.

---

## 1. Стек и решения

| Что | Решение |
|---|---|
| Язык | C#, **.NET 10 (LTS)**. .NET 8 не брать: поддержка заканчивается в ноябре 2026 |
| Цели | Windows 10 22H2 x64 и Windows 11 x64. ARM64 позже |
| Публикация | self-contained, `PublishSingleFile`, без требования ставить рантайм |
| Ядро | официальный `xray.exe` + `wintun.dll` из релиза XTLS/Xray-core (win-64 zip), версия закреплена, sha256 сверяется |
| Перехват | TUN-инбаунд Xray (Wintun) + маршруты, которые ставит служба |
| Привилегии | Windows-служба от LocalSystem; GUI и CLI без UAC, через named pipe |
| Установщик | Inno Setup (`.exe`), собирается в GitHub Actions (`windows-latest`) |
| GUI | WinForms: значок в трее + главное окно |

Не делаем (сознательно):
- legacy-транспорты `type=quic` / `type=http` (ядро v1.8.24): в нём нет TUN-инбаунда. Такая ссылка → понятная ошибка;
- режим шлюза для других устройств LAN;
- выборочную маршрутизацию по спискам доменов (возможно, позже: на Linux её тоже нет, это полный туннель);
- Windows 7/8.

---

## 2. Структура решения

```
src/
  VlessTunnel.Core/      разбор ссылки, сборка config.json, модели, общие константы
  VlessTunnel.Service/   служба: Xray, TUN, маршруты, kill-switch, DNS, watchdog, IPC-сервер
  VlessTunnel.Cli/       vless-tunnel.exe — команды через IPC
  VlessTunnel.Tray/      трей + окно, клиент IPC
  VlessTunnel.Native/    P/Invoke: IP Helper (iphlpapi), WFP (fwpuclnt)
tests/
  VlessTunnel.Core.Tests/   юнит-тесты + golden-тесты паритета с Linux
  parity/                   ссылки и эталонные JSON из Linux-бэкенда
installer/
  vless-tunnel.iss
.github/workflows/
  build.yml                 сборка, тесты, установщик, релиз по тегу
```

Пути на машине:

| Что | Где |
|---|---|
| Программа | `%ProgramFiles%\vless-tunnel\` (exe, xray.exe, wintun.dll) |
| Конфиг и ссылка | `%ProgramData%\vless-tunnel\` — ACL только SYSTEM + Administrators (в ссылке секретный UUID) |
| Сгенерированный config.json | там же, пересобирается при каждом старте |
| Логи | `%ProgramData%\vless-tunnel\logs\` с ротацией + Event Log для службы |

---

## 3. Архитектура

### 3.1 Разбор ссылки и config.json (Core)

Портировать Python-бэкенд из `vless-tunnel.sh` (функции `parse`/`make`,
примерно строки 290–760) **один в один**:

- транспорты: `tcp`/`raw`, `ws`, `grpc` (+`mode=multi`), `httpupgrade`, `xhttp`/`splithttp` (+`mode`, `extra` как JSON), `kcp` без header/seed;
- REALITY: `pbk`, `sid`, `spx`, `fp`, **`pqv` → `mldsa65Verify`**;
- TLS: `sni`, `fp`, `alpn`, `ech`; `allowInsecure` → получить отпечаток сертификата и прописать `pinnedPeerCertSha256` (как `cert-pin` на Linux);
- **`encryption=`** передаётся в `users[].encryption` без изменений;
- `flow=xtls-rprx-vision` → клиентский `xtls-rprx-vision-udp443` (опция отключения);
- IPv6-хост в `[...]`, ссылка без схемы, многострочная вставка с разрывами.

Отличия от Linux в config.json:
- инбаунд `tun` вместо `dokodemo-door` + TPROXY;
- SOCKS5 `127.0.0.1:10808` (с UDP) и HTTP `127.0.0.1:10809` — как на Linux;
- **адрес сервера в `vnext` — уже разрешённый IP** (см. 3.2), SNI/Host остаются доменом;
- правило `port 53 → dns-out`, DNS Xray — DoH через туннель;
- приватные сети → `direct` (опция `--proxy-lan` отключает).

**Паритет обязателен** (порядок работы — 5.7). Golden-тесты: набор ссылок (включая PQ, REALITY+pqv,
XHTTP+extra, vision, IPv6-хост, битые ссылки) прогоняется через Linux-бэкенд
(`vless-tunnel.sh`, подкоманда `make`), эталонные JSON коммитятся в
`tests/parity/`. C#-сборщик должен давать тот же outbound-блок байт в байт
(после нормализации JSON). Инбаунды сравниваются отдельно.

### 3.2 Перехват трафика и петля (Service)

Встроенный TUN Xray только поднимает адаптер. Адреса, маршруты и правила
делает ОС, то есть служба. Индекс интерфейса меняется при каждом запуске
Xray, поэтому ищем его заново каждый раз.

Порядок старта:
1. **Bootstrap-резолв** домена сервера самой службой (системный резолвер, до поднятия TUN). IP подставляется в config.json. Прямой DNS-исходящий Xray для bootstrap не нужен.
2. Запомнить текущий физический интерфейс и шлюз по умолчанию (`GetBestRoute2`).
3. Запустить `xray.exe` (Job Object: при смерти службы умирает и Xray).
4. Дождаться адаптера (`GetAdaptersAddresses`, по имени из конфига), таймаут.
5. Назначить адрес TUN (`CreateUnicastIpAddressEntry`), например `172.19.0.1/30`; для IPv6 — ULA из `fd00::/8` (без маршрутизируемого адреса IPv6 через TUN не пойдёт).
6. Маршруты (`CreateIpForwardEntry2`):
   - хост-маршрут до IP сервера через физический шлюз — **это и есть защита от петли** для proxy-исходящего;
   - `0.0.0.0/1` и `128.0.0.0/1` через TUN (не трогаем штатный default route);
   - IPv6: `::/1` и `8000::/1` через TUN, либо блок IPv6 при `--block-ipv6`;
   - приватные сети остаются на физическом интерфейсе (более специфичные маршруты выигрывают).
7. DNS: DNS-сервер адаптера TUN = адрес внутри TUN, низкая метрика TUN.
8. Включить kill-switch (3.3).

Отслеживать `NotifyRouteChange2` / `NotifyIpInterfaceChange` (смена Wi-Fi,
переподключение кабеля, сон): пересчитать физический шлюз и переставить
хост-маршрут до сервера. Периодически перерезолвить домен сервера; при
смене IP — перезапуск.

Остановка — строго в обратном порядке. Все созданные маршруты и фильтры
помечаются и снимаются по метке, не «по памяти процесса».

### 3.3 Kill-switch (обязателен)

На Linux kill-switch получается «по построению». **На Windows — нет:** если
Xray упал, адаптер TUN исчезает вместе с маршрутами, и трафик молча уходит
напрямую. Поэтому kill-switch через WFP — часть MVP, а не опция.

WFP, свой provider + sublayer с высоким весом, **постоянные** (не dynamic)
фильтры, чтобы пережить падение службы:
- разрешить: `xray.exe` (по app id), интерфейс TUN, loopback, DHCP, приватные сети (если не `--proxy-lan`);
- запретить: всё остальное исходящее, IPv4 и IPv6;
- DNS-утечки: запретить порт 53 на всех интерфейсах, кроме TUN (Windows параллельно шлёт DNS во все интерфейсы).

Снимаются фильтры только явным `off` или `uninstall`. `doctor` должен
уметь их найти и снять («аварийный выключатель» на случай поломки).

### 3.4 IPC и права (аналог sudoers)

Named pipe `\\.\pipe\vless-tunnel`, ACL: SYSTEM, Administrators и
пользователь, указанный при установке (аналог `--allow-user`). Протокол —
JSON-строки: запрос/ответ + **подписка на события** (смена состояния). Трей
не опрашивает службу по таймеру (урок Linux-версии, где опрос раз в 5 с
давал сотни вызовов).

### 3.5 CLI (`vless-tunnel.exe`)

Те же команды, что на Linux, где применимо:

```
on | off | toggle | restart
status [--json]
test            HTTP, SOCKS5, прозрачный TCP, UDP/QUIC, DNS (включая проверку утечки)
logs [--lines N]
set-link [--stdin]
autostart on|off
update-core     скачать релиз Xray, сверить .dgst, откат при неудаче
doctor          окружение + вердикт + снятие зависших WFP-фильтров
ensure          переставить маршруты/фильтры
uninstall [--keep-config]
```

Таймауты клиента IPC должны быть **больше** худшего пути команды в службе
(урок Linux: `toggle` с таймаутом 30 с при пути `on` > 31 с).

### 3.6 Трей и окно

- значок: вкл / выкл / неизвестно (иконки из `packaging/icons` Linux-версии);
- меню: включить/выключить, статус, вставить ссылку, логи, выход;
- окно: ссылка (поле скрывает UUID), статус, кнопка «Проверить», журнал;
- автозапуск трея — ярлык в Startup для пользователя;
- ошибки показывают и stdout, и stderr (урок Linux: `out or err` прятал stderr).

### 3.7 Установщик (аналог .deb)

- ставит файлы, регистрирует службу (автозапуск по опции), ярлыки, трей;
- **обновление поверх**: остановить службу → заменить файлы → запустить; конфиг сохраняется;
- один путь к бинарнику, никаких копий (урок Linux: дубль `/usr/bin` и `/usr/local/bin`);
- `uninstall` из CLI не удаляет файлы, принадлежащие установщику, а только сбрасывает настройку; полное удаление — через «Программы и компоненты»;
- удаление снимает службу, маршруты и WFP-фильтры;
- версия Xray и wintun зашиты в сборку, sha256 проверяется в CI.

### 3.8 Подпись и доверие (самоподписанный сертификат)

Порядок на машине пользователя:
1. `trust.ps1` (один раз, от администратора): сверяет отпечаток `vless-tunnel.cer` с зашитым в скрипт, ставит сертификат в `LocalMachine\Root` и `LocalMachine\TrustedPublisher`, снимает метку «из интернета» (`Unblock-File`) с установщика и запускает его.
2. Установщик: установка или обновление поверх. Повторно `trust.ps1` не нужен.
3. Удаление через «Программы и компоненты» убирает всё, включая сертификат; настройки — по вопросу (см. ниже).

Сертификат: `New-SelfSignedCertificate -Type CodeSigningCert` (только подпись
кода). `.pfx` хранится только в секретах GitHub Actions и в офлайн-копии,
никогда не в репозитории. Подписываются все свои `.exe` и установщик
(директива `SignTool` в Inno Setup, включая деинсталлятор), всегда с меткой
времени (`/tr … /td SHA256`).

Деинсталлятор (Inno Setup):

```ini
[UninstallRun]
; настройки удаляет сам деинсталлятор по ответу пользователя, CLI их не трогает
Filename: "{app}\vless-tunnel.exe"; Parameters: "uninstall --keep-config"; Flags: runhidden waituntilterminated; RunOnceId: "Purge"
Filename: "{sys}\certutil.exe"; Parameters: "-delstore Root <THUMBPRINT>"; Flags: runhidden; RunOnceId: "DelRoot"
Filename: "{sys}\certutil.exe"; Parameters: "-delstore TrustedPublisher <THUMBPRINT>"; Flags: runhidden; RunOnceId: "DelTP"

[UninstallDelete]
; логи удаляются всегда
Type: filesandordirs; Name: "{commonappdata}\vless-tunnel\logs"

[Code]
var
  DeleteSettings: Boolean;

procedure CurUninstallStepChanged(CurUninstallStep: TUninstallStep);
begin
  case CurUninstallStep of
    usUninstall:
      begin
        { тихое удаление (/SILENT, /VERYSILENT) настройки сохраняет }
        DeleteSettings := False;
        if not UninstallSilent() then
          DeleteSettings := MsgBox(
            'Удалить настройки vless-tunnel (ссылку на сервер и параметры)?' + #13#10 + #13#10 +
            'Нет — настройки останутся и подхватятся при следующей установке.',
            mbConfirmation, MB_YESNO or MB_DEFBUTTON2) = IDYES;
      end;
    usPostUninstall:
      if DeleteSettings then
        DelTree(ExpandConstant('{commonappdata}\vless-tunnel'), True, True, True);
  end;
end;
```

Требования:
- вопрос задаётся после подтверждения удаления и до снятия файлов; по умолчанию выбран «Нет»;
- обновление поверх деинсталлятор не запускает — сертификат и настройки не трогаются; обновление через «удалить → поставить» не делать;
- при установке служба подхватывает существующий `%ProgramData%\vless-tunnel`, если он остался;
- один сертификат — один проект: удаление снимает доверие со всего, что им подписано;
- при смене сертификата (истёк срок) — новый `trust.ps1` с новым отпечатком; старые сборки остаются валидными благодаря метке времени.

Известное: даже подписанный файл, скачанный из интернета, SmartScreen может
показывать с предупреждением (проверка по репутации), поэтому `trust.ps1`
снимает метку `Unblock-File`. Прокси-клиенты иногда ложно ловит антивирус.

---

### 3.9 Эксплуатация: то, что легко упустить

**Аварийное восстановление сети.** Постоянные WFP-фильтры переживают всё,
в том числе ручное удаление папки программы. Если `vless-tunnel.exe` уже нет,
`doctor` не запустить, и машина остаётся без интернета. Поэтому:
- фильтры создаются с фиксированным GUID провайдера и sublayer (константы в Core);
- отдельный самодостаточный `rescue.ps1` (P/Invoke через `Add-Type`, без зависимостей от программы) снимает всё по этим GUID; лежит в репозитории, ссылка — в README и в меню «Пуск»;
- ярлык «Аварийно отключить vless-tunnel» в меню «Пуск».
Альтернатива WFP — правила брандмауэра Windows (снимаются штатным
`Remove-NetFirewallRule`), но они не работают, если сторонний антивирус
подменил брандмауэр. ~~Выбор сделать на этапе 0.~~ Решено на этапе 0б:
**WFP** — переживает замену брандмауэра антивирусом и падение службы
(постоянные фильтры), обычные правила брандмауэра — нет. Подробности и
обоснование — `docs/spike.md`.

**Captive portal** (Wi-Fi в гостинице, кафе). Kill-switch не пустит на
страницу входа. Нужна команда/пункт трея «Пустить на 5 минут напрямую»:
временно снимает блокировку, по таймеру возвращает, туннель не включён.

**Обновление поверх.** Файлы заняты работающими службой и треем. В
`PrepareToInstall` (Inno Setup) остановить службу и закрыть трей всех
пользователей, после установки запустить обратно. Kill-switch на время
обновления остаётся включённым, если туннель был включён.

**Обновление программы (`self-update`).** Проверка релизов на GitHub,
скачивание установщика, сверка sha256 **и** отпечатка подписи с зашитым,
запуск установщика тихо (`/VERYSILENT` — настройки сохраняются, см. 3.8).

**Смена сертификата.** `trust.ps1` содержит список старых отпечатков и
удаляет их из `Root`/`TrustedPublisher` при установке нового. Деинсталлятор
удаляет текущий и все старые.

**Запуск `trust.ps1`.** Скачанный `.ps1` сам помечен «из интернета», а
политика выполнения по умолчанию запрещает скрипты. Нужна обёртка
`install.cmd`: `powershell -NoProfile -ExecutionPolicy Bypass -File "%~dp0trust.ps1"`.

**Кодировка.** Windows PowerShell 5.1 читает `.ps1` без BOM как ANSI —
кириллица в сообщениях превратится в мусор. Все `.ps1` — **UTF-8 с BOM**
(проверка в CI). CLI выставляет `Console.OutputEncoding = UTF8`.

**Секреты в выводе.** UUID и ключи не печатаются в логах, `status`, `doctor`
и окне (маскировать). Конфиг-директория с ACL (3.2 / раздел 2).

**Устойчивость службы.** Действия при сбое (`sc failure`): перезапуск
через 5 с, 30 с, 60 с. Xray в Job Object. После сна и гибернации — полная
проверка маршрутов и адаптера, не только по уведомлениям.

**Время.** REALITY и TLS ломаются при сбитых часах (часто в виртуалках).
`doctor` сверяет время (NTP или заголовок `Date` через туннель) и
предупреждает.

**MTU.** TLS + XHTTP добавляют накладные расходы; MTU TUN 1500 может давать
фрагментацию и «висящие» сайты. Подобрать значение на этапе 0 (ориентир
1400), сделать опцией.

**Совместимость** (аналог раздела в Linux README). Проверить и описать:
другой VPN/TUN (v2rayN, WireGuard, OpenVPN), WSL2, Hyper-V, Docker Desktop
(их трафик идёт через NAT хоста — туннелируется ли и не режет ли его
kill-switch), сторонний антивирус с собственным брандмауэром.

**Лицензии.** `THIRD_PARTY_LICENSES/` как в Linux-репозитории: Xray-core
(MPL-2.0), Wintun (лицензия на готовые бинарники), .NET-зависимости.
Установщик кладёт их в `{app}`.

**Windows 10.** Плановая поддержка закончилась в октябре 2025 года —
основной целью считать и Windows 11, тестировать на обеих.

## 4. Этапы и критерии приёмки

Каждый этап — отдельный PR. Тестирование — на стенде из раздела 5;
опасные тесты — только через `with-rescue.sh`.

**Этап 0а. Стенд.** Виртуалка `vt-win10` создаётся скриптом без участия человека; SSH по ключу и guest agent работают; снимок `clean` есть; `rescue.ps1`, `run-test.ps1`, `with-rescue.sh` готовы; цепочка спасения проверена: намеренно отрезанная сеть возвращается через guest agent, а при «мёртвом» guest agent — откатом снимка.

**Этап 0б. Спайк (без C#, PowerShell).** Это ворота для всего остального. Все эксперименты — через `with-rescue.sh`.
- `xray.exe` + `wintun.dll` + TUN-инбаунд поднимают адаптер;
- ручные маршруты по 3.2 → трафик идёт через сервер, петли нет;
- тестовая ссылка с PQ-параметрами работает;
- проверить IPv6 через TUN с ULA-адресом;
- проверить, как ведёт себя система при `taskkill` Xray (подтвердить утечку без kill-switch);
- выбрать механизм kill-switch: WFP или правила брандмауэра (3.9), проверить способ аварийного снятия;
- подобрать MTU TUN;
- проверить сон/гибернацию и смену Wi-Fi ↔ кабель.
Результат — `docs/spike.md` с командами и выводами.

**Этап 1. Core + тесты.** Все golden-тесты паритета зелёные, битые ссылки дают понятные ошибки.

**Этап 2. Служба: Xray, TUN, маршруты.** `on`/`off` через временный отладочный вход; переключение Wi-Fi ↔ кабель не рвёт туннель надолго; после `off` таблица маршрутов как до `on`.

**Этап 3. Kill-switch и DNS.** `taskkill /f xray.exe` → интернета нет, пока туннель не поднимется; утечек DNS нет (проверка dnsleaktest-подобным запросом и захватом трафика на физическом интерфейсе); `doctor` снимает фильтры.

**Этап 4. IPC + CLI.** Все команды из 3.5; пользователь без прав администратора управляет туннелем; чужой пользователь — нет.

**Этап 5. Трей и окно.** Состояние обновляется по событиям, без опроса.

**Этап 6. Установщик и CI.** Чистая установка через `trust.ps1`, обновление поверх (настройки и сертификат на месте), удаление с ответом «Да» и «Нет» и тихое удаление — на свежем снимке виртуалки; после удаления сертификата нет в `Root` и `TrustedPublisher`. Релиз по тегу выкладывает `.exe` и `sha256`.

**Этап 7. Сервис-команды.** `test`, `doctor` (включая проверку времени), `update-core` с откатом, `self-update` со сверкой подписи, режим captive portal, `rescue.ps1` снимает фильтры на машине без установленной программы, логи с ротацией, секреты не попадают в вывод.

---

## 5. Стенд: агент на Ubuntu, Windows в виртуалке

Агент работает на хосте с Ubuntu и сам создаёт и ведёт тестовую виртуалку
Windows (KVM/libvirt). Ключевое свойство: **сеть агента от тестов не
зависит.** Сломанные маршруты или kill-switch отрезают только виртуалку, а
агент снаружи всегда может её спасти или откатить.

### 5.1 Что делает владелец (один раз)

У агента есть passwordless sudo на хосте (`NOPASSWD:ALL`, настроено владельцем
через `/etc/sudoers.d/`, см. 5.8) — установку пакетов, группы и проверку
`virsh` агент теперь выполняет сам:

```sh
egrep -c '(vmx|svm)' /proc/cpuinfo        # > 0, виртуализация включена в BIOS
sudo apt install qemu-kvm libvirt-daemon-system virtinst genisoimage ovmf swtpm
sudo usermod -aG libvirt,kvm $USER        # затем перелогиниться
virsh -c qemu:///system list              # должно работать без sudo
```

Владелец вручную делает только то, что требует браузера/интерактивного входа
или является его личным решением:

- скачивает ISO Windows 10 22H2 x64 с сайта Microsoft и кладёт в `~/vm-iso/win10.iso` (страница Microsoft требует ручных шагов, автоматически не скачивается);
- ставит .NET 10 SDK на хост (или поручает агенту через `dotnet-install.sh` — на усмотрение);
- проверяет, не заворачивает ли Linux-клиент vless-tunnel на хосте трафик виртуалки (NAT через `virbr0`) в свой туннель. Если заворачивает — отключить на время тестов или учитывать «туннель в туннеле» при оценке результатов. Записать вывод в раздел «Статус».

### 5.2 Параметры виртуалки

| Что | Значение |
|---|---|
| Имя | `vt-win10` |
| ОС | Windows 10 22H2 x64 (Windows 11 — позже, отдельной машиной) |
| Прошивка | BIOS (SeaBIOS) — ради внутренних снимков qcow2; UEFI + TPM нужны только для Windows 11 и усложняют снимки |
| Ресурсы | 4 vCPU, 8 ГБ RAM, диск qcow2 64 ГБ |
| Сеть | libvirt `default` (NAT, `virbr0`) |
| Драйверы и guest agent | ISO virtio-win (агент скачивает сам с fedorapeople.org, сверяет контрольную сумму) |
| Установка | без участия человека: `autounattend.xml` на отдельном ISO |

Секреты стенда (пароль администратора виртуалки, SSH-ключ) — в
`~/.config/vless-tunnel-dev/`, не в репозитории. Пароль генерируется при
создании.

`autounattend.xml` и скрипт первого входа (`setup-guest.ps1`) должны:
- создать локальную учётку администратора; автологин — постоянный (по решению владельца: это стенд для разработки, а не конечный продукт, вводить пароль на консоли каждый раз неудобно). Технически это ещё и единственный работающий способ запустить `setup-guest.ps1` без участия человека в этом образе: `Microsoft-Windows-Deployment`/`RunSynchronousCommand` не поддерживается этой сборкой Windows ни в `specialize`, ни в `oobeSystem` (проверено эмпирически через `setupact.log`), поэтому используется `FirstLogonCommands`, а для него нужен хотя бы один вход;
- поставить драйверы virtio и **qemu-guest-agent** с ISO virtio-win;
- включить **OpenSSH Server**, PowerShell как оболочку по умолчанию, вход по ключу (`administrators_authorized_keys` с правильным ACL);
- отключить сон и гибернацию (иначе автоматические тесты будут прерываться простоем) — но **не** Windows Update: владелец настоял, что стенд должен вести себя как обычная машина пользователя, иначе часть реальных проблем (обновление посреди теста, перезагрузка, столкновение с туннелем) просто не всплывёт до релиза;
- исключить `C:\dev` из проверки Defender (только эта папка со скриптами стенда, не сам vless-tunnel — тот ставится в `%ProgramFiles%\vless-tunnel\` и остаётся под обычной проверкой антивируса, как и должно быть);
- создать `C:\dev\{logs,secrets,bin}`;
- в конце выключить машину — это сигнал хосту, что установка завершена.

### 5.3 Файлы стенда в репозитории

```
vm/
  create-vm.sh          создать vt-win10 с нуля (идемпотентно: есть — не трогать)
  autounattend.xml      шаблон, пароль и ключ подставляются при сборке ISO
  setup-guest.ps1       первичная настройка Windows
  vmctl.sh              ssh | scp | exec (через guest agent) | ip | snapshot | revert | rescue | status
  with-rescue.sh        обёртка опасного теста (5.5)
```

`vmctl.sh` — единственная точка управления: агент не пишет длинные
`virsh`/`ssh` вручную. IP виртуалки — через `virsh domifaddr` или guest agent.

Выполнение команды через guest agent (работает без сети внутри виртуалки):

```sh
virsh qemu-agent-command vt-win10 '{"execute":"guest-exec","arguments":{
  "path":"powershell.exe",
  "arg":["-NoProfile","-ExecutionPolicy","Bypass","-File","C:\\dev\\rescue.ps1"],
  "capture-output":true}}'
# → {"return":{"pid":N}}
virsh qemu-agent-command vt-win10 '{"execute":"guest-exec-status","arguments":{"pid":N}}'
# вывод приходит в base64
```

### 5.4 Снимки

- `clean` — сразу после установки и настройки. **Никогда не удалять.**
- `stage-N` — перед началом каждого этапа.
- `pre-<тест>` — перед каждым опасным тестом; старые `pre-*` агент удаляет сам, оставляя последние 3.

Агент откатывает снимки сам. Откат к `clean` — только если текущий снимок
этапа тоже испорчен; записать причину в «Статус».

### 5.5 Опасные тесты и спасение

Опасный тест — всё, что меняет маршруты, DNS, брандмауэр/WFP или запускает
туннель внутри виртуалки.

**`rescue.ps1`** (внутри виртуалки, первый артефакт этапа 0) —
идемпотентный, не зависит от программы, снимает всё: службу и `xray.exe`,
маршруты и адрес TUN, DNS интерфейсов, WFP-фильтры по GUID (3.9) и правила
брандмауэра по группе, задачу `vt-rescue`. Пишет в
`C:\dev\logs\rescue-<время>.log`, что именно снял.

**`run-test.ps1`** (внутри виртуалки) — один скрипт на тест: применить →
проверить → записать `RESULT: PASS/FAIL` в лог → откатить в `finally`:

```powershell
param([Parameter(Mandatory)][string]$Name, [Parameter(Mandatory)][string]$Script)
$log = "C:\dev\logs\$Name-$(Get-Date -f yyyyMMdd-HHmmss).log"
Start-Transcript $log
try     { & $Script }                       # печатает RESULT: ...
catch   { "RESULT: ERROR $_" }
finally { & C:\dev\rescue.ps1; Stop-Transcript }
```

Проверки связи — всегда с таймаутом (`curl.exe -s --max-time 10 …`).
Тесты, где сеть **должна** пропасть (kill-switch), устроены так же: убить
Xray → убедиться, что `curl` **не** проходит → записать → откатить.

**`with-rescue.sh`** (на хосте) — обёртка, через которую агент запускает
любой опасный тест. Цепочка спасения:

1. снимок `pre-<тест>`;
2. внутри виртуалки ставится задача `vt-rescue` (через 10 минут сама запускает `rescue.ps1`; около полуночи задавать дату `/SD`);
3. `run-test.ps1` запускается через SSH под `timeout`; лог копируется на хост в `logs/vm/`;
4. проверка: SSH отвечает и из виртуалки есть интернет (`curl` без туннеля);
5. если SSH не отвечает → `rescue.ps1` через **guest agent**, ждать до 60 с, проверить снова;
6. если guest agent не отвечает или сеть не вернулась → `virsh snapshot-revert pre-<тест>`, отметить тест как `BROKE_VM`;
7. при успехе — снять задачу `vt-rescue`.

Логи — единственный источник правды о том, что происходило без связи:
результат пишется **до** отката, при неудаче агент начинает с чтения лога, а
не с повторного запуска.

Доступ с хоста при включённом kill-switch: подсеть `virbr0` — частная и
разрешена правилами (3.3), ответы на входящий SSH не блокируются. В режиме
`--proxy-lan` SSH может пропасть — это ожидаемо, спасение через guest agent.

### 5.6 Сборка и доставка на стенд

- C#-проекты собираются на хосте (`EnableWindowsTargeting=true`), юнит-тесты Core запускаются тоже на хосте;
- артефакты копируются в `C:\dev\bin` через `vmctl.sh scp`;
- установщик Inno Setup компилируется внутри виртуалки (Inno Setup ставится туда тихо) — для разработки; релизная сборка — в GitHub Actions;
- тестовая ссылка (отдельный UUID на сервере, не основной) лежит на хосте в `~/.config/vless-tunnel-dev/test-link.txt`, копируется в `C:\dev\secrets\` и в логи не попадает.

### 5.7 Golden-тесты паритета

Агент сам генерирует эталонные JSON на хосте из Linux-бэкенда
(`~/src/vless-tunnel`, подкоманда `make`), коммитит в `tests/parity/` вместе
со списком ссылок и версией эталона. Реальные UUID и ключи в эталонах
заменяются тестовыми.

### 5.8 Границы агента

- на хосте: работать только в каталоге репозитория, `~/src/vless-tunnel` (только чтение), `~/vm-iso`, `~/.local/share/vt-vm/` (диски, ISO сборки) и `~/.config/vless-tunnel-dev/`; passwordless sudo (`NOPASSWD:ALL`) есть, но использовать только для настройки стенда (пакеты, группы, libvirt/virsh), не для действий вне этих каталогов и не для операций с виртуалкой в обход `with-rescue.sh`;
- libvirt: трогать только домен `vt-win10` и его снимки; сеть `default` не менять;
- в виртуалке: права администратора, работа в `C:\dev`;
- GitHub: токен только на этот репозиторий.

### 5.9 Статус

После каждого этапа агент обновляет раздел «Статус» в конце файла: что
сделано, что проверено (со ссылкой на лог), что не получилось, какой снимок
актуален, следующий шаг. Новая сессия начинает с него.

### 5.10 Стартовое сообщение агенту

```
Ты работаешь на Ubuntu, у тебя есть passwordless sudo (используй его для
настройки стенда, не выходя за границы раздела 5.8). Прочитай docs/PLAN.md целиком,
особенно раздел 5. Эталон: ~/src/vless-tunnel
(https://github.com/RamDll/vless-tunnel) — переноси поведение, код не копируй.

Этап 0а: создай стенд по разделу 5 (vm/*), установи Windows 10 из
~/vm-iso/win10.iso без участия человека, проверь SSH и guest agent,
сделай снимок clean. Затем напиши rescue.ps1, run-test.ps1 и
with-rescue.sh и проверь всю цепочку спасения, намеренно отрезав
виртуалке сеть.

Любые изменения сети внутри виртуалки — только через with-rescue.sh.
Не трогай другие домены libvirt и ничего вне разрешённых каталогов.
Если что-то неясно или противоречиво — спроси. После этапа обнови «Статус».
```

## 6. Открытые вопросы владельцу

1. Отдельный репозиторий (`vless-tunnel-windows`) или папка в `vless-tunnel`?
2. ~~Подписывать ли установщик?~~ Решено: самоподписанный сертификат + `trust.ps1` (раздел 3.8).
3. Нужен ли IPv6 в MVP или достаточно `--block-ipv6` по умолчанию?
4. Автозапуск службы при загрузке — включён по умолчанию или опция?
5. Kill-switch включён всегда или отключаемый (как `--no-kill-switch`)?
6. Язык интерфейса: только русский или русский + английский?
7. Нужна ли вторая машина стенда с Windows 11 (UEFI + TPM) и на каком этапе?

---

## Статус

_Заполняет агент после каждого этапа (см. 5.9)._

- Этап 0а (стенд): **готово полностью.**
  - Хост подготовлен: `qemu-system-x86`/`libvirt-daemon-system`/`virtinst`/`genisoimage`/`ovmf`/`swtpm`/`libguestfs-tools`/`wimtools`/`libosinfo-bin` установлены, `libvirtd` активен, сеть `default` (NAT `virbr0`) поднята, группы `libvirt`/`kvm` у пользователя. .NET 10 SDK 10.0.112 через apt.
  - `vm/*` написаны и работают: `create-vm.sh`, `autounattend.xml.template`, `setup-guest.ps1`, `rescue.ps1`, `run-test.ps1`, `vmctl.sh`, `with-rescue.sh`.
  - **Виртуалка `vt-win10` полностью устанавливается с нуля без участия человека**: `autounattend.xml` (Windows 10 Pro, ru-RU, диск SATA/BIOS/MBR, автологин vtadmin), `setup-guest.ps1` через `FirstLogonCommands` ставит guest agent, виртио-драйверы, OpenSSH (вход по ключу), выключает сон/гибернацию, Defender-исключение для `C:\dev`, копирует `rescue.ps1`/`run-test.ps1` — и сама выключается в конце (сигнал хосту). SSH и guest agent подтверждены (`vmctl.sh status` → `ssh: ok`). Снимок `clean` снят.
  - Windows Update **сознательно не трогаем** (решение владельца, см. 5.2) — стенд ведёт себя как обычная машина пользователя.
  - **Цепочка спасения проверена вживую, оба сценария:**
    - обрыв сети (`Disable-NetAdapter` через guest-exec) → `vmctl.sh rescue` (тот же `rescue.ps1` через guest agent) → SSH снова доступен;
    - обрыв сети **и** убитый `QEMU-GA` одновременно (полный отказ) → `virsh snapshot-revert clean --running` → guest agent и SSH снова живы.
    - Дополнительно обнаружено: `run-test.ps1`'s `finally` сам вызывает `rescue.ps1` **локально**, независимо от хоста — это дополнительный (более быстрый) уровень защиты поверх host-side пути через `with-rescue.sh`, оба работают.
  - `vmctl.sh` доработан: `-o ServerAliveInterval=5 -o ServerAliveCountMax=2` для ssh/scp — без этого при реальном обрыве сети (не просто недоступности порта, а падения соединения на живом TCP) ssh мог зависать на много минут вместо быстрого обнаружения обрыва.
  - **Известные грабли, на будущее:**
    - `Microsoft-Windows-Deployment`/`RunSynchronousCommand` не работает в этой сборке Windows ни в `specialize` (жёсткая ошибка парсинга файла ответов), ни в `oobeSystem` (тихо регистрируется и игнорируется) — рабочий путь: `FirstLogonCommands` + постоянный `AutoLogon`.
    - `$OEM$\$1\...` не копируется Setup'ом, если ответный файл лежит на ОТДЕЛЬНОМ CD-ROM, а не на самом install-ISO (нужен WDS/MDT-подобный сценарий) — файлы для `FirstLogonCommands` кладутся в корень ресурсного ISO, буква диска ищется перебором (`for %d in (C..J)`) прямо в команде.
    - Спецсимволы `%date%`/`%time%`, `&`, скобки, `>>` в `<Path>`/`<CommandLine>` ломают разбор файла ответов — держать команду максимально простой.
    - `.ps1`-файлы **обязательно** с UTF-8 BOM (уже знали из раздела 3.9, но один раз забыли применить к своим же файлам) — без BOM Windows PowerShell 5.1 не парсит кириллицу и падает на `ParserError`.
    - QEMU-канал guest agent (`--channel unix,target_type=virtio,name=org.qemu.guest_agent.0`) нужно добавлять в `virt-install` явно, сам не появляется.
    - Все диски/CD-ROM у одной VM должны быть на одной шине (`bus=sata`) с явным `boot_order` — смешение с `bus=ide` (куда `--cdrom` сажает медиа по умолчанию) путает SeaBIOS.
    - `libvirt-qemu` не имеет доступа к файлам в домашней папке — в `/etc/libvirt/qemu.conf` выставлено `user = "ramdll"`.
    - `vmctl.sh` требует `-o IdentitiesOnly=yes` для ssh/scp — иначе ssh-agent хоста подставляет чужие ключи раньше нашего и упирается в лимит попыток сервера.
    - Диагностика живой VM без SSH/agent — через `virsh screenshot` + `virsh send-key` + QMP `input-send-event` (абсолютная мышь, есть usb-tablet) для скриншотов/кликов/набора текста вслепую; офлайн-чтение диска через `virt-ls`/`virt-cat` (`libguestfs-tools`) — надёжнее живой сессии, не зависит от прав/блокировок.
- Этап 0б (спайк): **готово, см. `docs/spike.md`.**
  - **Подтверждено — сквозной трафик через TUN работает.** `xray.exe`+
    `wintun.dll` поднимают адаптер, адрес/анти-петлевой маршрут/
    `0.0.0.0/1`+`128.0.0.0/1` настраиваются вручную (как будет делать
    Service, `autoRoute: false`), и трафик реально доходит до Xray и
    проксируется через **настоящую тестовую PQ-ссылку**
    (REALITY+xhttp+vision, `~/.config/vless-tunnel-dev/test-link.txt`)
    на реальный сервер 130.17.21.198 — подтверждено логом Xray построчно.
    Ночная находка «трафик не доходит» была артефактом теста (проверка
    сразу после добавления маршрута, до его применения Windows) — важный
    урок для Service: нужна пауза/retry после установки маршрутов перед
    тем, как считать туннель поднятым.
  - Побочная находка: адаптер Xray на Windows регистрируется как
    `NdisMedium = MBB`, из-за чего Windows не считает его Remote Access
    Interface (известный issue XTLS/Xray-core#6019) — не мешает
    трафику, но значит для kill-switch (3.3/3.9) фильтровать по
    имени/GUID адаптера, не по типу интерфейса.
  - **IPv6 через TUN с ULA** (`fd00::1/64`) — подтверждено, лог Xray
    показывает реальное проксирование `[fd00::1]` → `[2606:4700:4700::1111]`
    через тот же сервер, параллельно с IPv4.
  - **`taskkill xray.exe` без kill-switch** — утечка подтверждена
    (адаптер/маршруты исчезают вместе с процессом, трафик после этого
    молча идёт напрямую) — ровно как предсказывал план (3.3).
  - **Kill-switch: выбор WFP подтверждён** (не меняем решение из 3.3) —
    обоснование в `docs/spike.md`; ссылка на решение убрана из открытых
    вопросов в 3.9.
  - **MTU 1400** подтверждён реальной передачей 3 МБ без фрагментации
    (не только TCP-handshake).
  - **Смена кабеля** (отключить/включить физический адаптер) — маршруты
    и туннель пережили цикл сами, без вмешательства; но это НЕ полная
    проверка смены Wi-Fi↔Ethernet (тот же интерфейс/шлюз, не другой) —
    честно отмечено как ограничение этой VM (один физический адаптер).
  - **Сон/гибернация — сознательно не тестировалось** в этой VM (KVM не
    эмулирует ACPI S3/S4 как реальное железо, результат был бы
    непоказательным) — отложено на этап 2 / реальное железо.
  - Xray-core v26.3.27 закреплён в `vm/pinned-versions.txt` (sha256
    подтверждён издателем, не только нами — в отличие от virtio-win).
- **`VlessTunnel.Core` — парсер ссылки, outbound-блок и полный `config.json` готовы.**
  - `src/VlessTunnel.Core/`: `LinkParser` (порт `parse_link`) и
    `OutboundBuilder` (порт `build_stream`/части `build_config`) —
    построчное соответствие эталону (`~/src/vless-tunnel`, v1.2.9)
    сознательно, ради паритета.
  - `tests/parity/`: 6 эталонных outbound-JSON сгенерированы РЕАЛЬНЫМ
    Linux-бэкендом (фейковые UUID/ключи, TEST-NET-адреса из RFC 5737) —
    PQ REALITY+xhttp (с `pqv`→`mldsa65Verify`), vision(+/-udp443), ws,
    grpc+multi, IPv6-хост. Golden-часть паритета с Linux не требуется
    и не проверяется для полного конфига — инбаунды принципиально
    другие (TUN вместо dokodemo-door+TPROXY, план 3.1 "Отличия от
    Linux"), поэтому DNS/routing вокруг них тоже другие.
  - `ConfigBuilder`/`WindowsConfigOptions` (новое): сборка полного
    Windows-конфига — TUN+SOCKS+HTTP инбаунды, DNS (только 2 DoH-
    резолвера, без `dns-direct-N` для домена сервера — служба
    резолвит его bootstrap'ом ДО сборки конфига, план 3.2 п.1),
    routing (dns-proxy→proxy, порт 53→dns-out, приватные сети→direct
    как defense-in-depth при `--proxy-lan` выкл, catch-all→proxy).
    Анти-петля на Windows — маршрут ОС (хост-роут через физический
    шлюз, подтверждено спайком), поэтому правила "адрес сервера→direct"
    внутри Xray, в отличие от Linux, здесь нет вовсе.
  - **Все 36 тестов зелёные** (24 прежних + 12 новых поведенческих для
    `ConfigBuilder`: форма TUN-инбаунда, пропуск IPv6-адреса при
    `null`, порядок/порты инбаундов, отсутствие domain-правила,
    порт 53→dns-out, вкл/выкл приватных сетей, catch-all→proxy, состав
    DNS-серверов, `ServerAddressOverride` в `vnext.address`,
    access-log пути, наличие всех секций конфига).
  - **Живая проверка на VM**: `config.json`, собранный ИМЕННО
    Core-библиотекой (не руками) из реальной секретной тестовой
    PQ REALITY+xhttp+Vision ссылки, загружен на стенд и запущен через
    `xray.exe` — реальное TCP-соединение через туннель прошло
    успешно, лог Xray подтвердил настоящее REALITY+XHTTP+Vision
    проксирование на реальный сервер. Это подтверждает не только
    unit-тесты, но и то, что Core-конфиг реально поднимает туннель.
  - Ещё не портировано: `build_state`/аналог state.json, всё, что
    относится к Service/CLI/Tray (этап 2+).
- **Этап 2 (начат) — `VlessTunnel.Native` (P/Invoke IP Helper) и
  `VlessTunnel.Service` (оркестрация, план 3.2).**
  - `VlessTunnel.Native`: `RouteManager` (адрес/маршруты TUN, поиск
    физического шлюза через GetBestRoute2), `RouteWatcher`
    (NotifyRouteChange2/NotifyIpInterfaceChange), `JobObject`
    (KILL_ON_JOB_CLOSE — xray.exe умирает вместе со службой).
  - Два реальных бага найдены и исправлены ДО стенда, юнит-тестами
    (`tests/VlessTunnel.Native.Tests`, 10 тестов на `Marshal.SizeOf`/
    `OffsetOf`): поле-заполнитель `ulong` вместо `char[8]` в
    `SOCKADDR_IN.sin_zero` и в `IN6_ADDR` задавало выравнивание 8 байт
    вместо настоящих 1–2 — .NET молча раздувал паддингом
    `SOCKADDR_INET` (28→32 байта) и сдвигал все поля после него в
    `MIB_IPFORWARD_ROW2` на 8 байт; вызов реального
    `CreateIpForwardEntry2` с таким layout'ом писал бы не туда.
    Исправлено на обычные byte-поля, размеры/смещения теперь совпадают
    с документированными для x64 (SOCKADDR_INET=28,
    MIB_UNICASTIPADDRESS_ROW=80, MIB_IPFORWARD_ROW2=104 байт).
  - `VlessTunnel.Service`: `BootstrapResolver` (план 3.2 п.1),
    `TunnelManager` — весь порядок из 3.2 (запомнить физический шлюз →
    собрать config.json с уже разрешённым IP → запустить xray.exe под
    Job Object → дождаться адаптера → адрес TUN → хост-маршрут до
    сервера + два `/1`-маршрута через TUN → слежение за сменой сети),
    откат строго в обратном порядке. Временный отладочный вход
    (`VlessTunnel.Service.exe run <ссылка> <xray.exe> <рабочая папка>
    [--duration N]`) — полноценный IPC/CLI ещё не строился, это этап 4.
  - **Живые прогоны на стенде (после фикса)**: три чистых цикла
    on→off (25с, 40с, 35с) — адаптер/адрес/анти-петлевой хост-маршрут
    поднимаются, реальный TCP через 1.1.1.1:443 проходит именно через
    TUN (проверено по наличию `/1`-маршрутов, не только по факту
    соединения), после `off` таблица маршрутов и список адаптеров
    возвращаются к состоянию до `on` (0 лишних записей).
  - **Найден и исправлен второй баг живым тестом**: `RouteWatcher`
    реагировал на СВОИ ЖЕ изменения таблицы маршрутов (добавление
    `/1`-маршрутов само порождает NotifyRouteChange2), из-за чего
    `RecomputeHostRoute` уходил в бесконечный цикл и в итоге перекидывал
    хост-маршрут до сервера через сам TUN — ту самую петлю, от которой
    он должен защищать. Исправлено окном подавления (2 с) после любой
    собственной мутации маршрутов/адресов.
  - **Третий баг найден и исправлен**: причина нестабильного времени
    появления TUN-адаптера (от ~1 с до таймаута в 40 с) — `Process.Start`
    с `RedirectStandardOutput/Error=true` без вычитывания потоков.
    Анонимные pipe'ы ОС имеют ограниченный буфер; когда он заполнялся
    логами xray.exe, процесс блокировался на записи в свой же stdout —
    а значит блокировалась и вся дальнейшая инициализация, включая
    поднятие TUN. Исправлено: `BeginOutputReadLine`/`BeginErrorReadLine`
    с обработчиками, пишущими строки xray в общий лог службы через
    `_log` (с префиксом `xray:`/`xray[err]:`) — pipe теперь вычитывается
    постоянно, асинхронно. Побочная находка при разборе логов xray:
    Windows шлёт много NetBIOS-широковещательных UDP:137-запросов через
    TUN сразу после его поднятия — ожидаемо (роутится в `direct` по
    правилу приватных сетей), но объясняет неожиданно большой объём
    логов при полном выводе xray.
  - **Подтверждено 5 подряд чистыми циклами on→off** (по 10 с) после
    фикса: адаптер поднимается за ~0–1 с КАЖДЫЙ раз, ни одного
    таймаута; после каждого `off` — 0 процессов xray.exe, 0 адаптеров
    xray0, таблица маршрутов чистая.
  - **Четвёртый баг найден и исправлен**: даже с окном подавления (2 с)
    после собственных мутаций, `RecomputeHostRoute` один раз словил
    отложенное событие (вероятно, Windows NLA профилирует новую сеть уже
    после появления TUN-адаптера) и снова получил от `GetBestRoute2`
    сам TUN как "лучший маршрут" к серверу — окно подавления по времени
    не универсально. Добавлен жёсткий инвариант, не зависящий от
    таймингов: если пересчитанный интерфейс == TUN-интерфейс, результат
    отбрасывается, восстанавливается предыдущий известный хороший шлюз.
  - **Живучесть при смене физического адаптера — подтверждена**
    (`Disable-NetAdapter`/`Enable-NetAdapter` посреди сеанса, на
    полностью чистом снимке `clean`, чтобы исключить накопленные
    артефакты вечера): при разрыве `RecomputeHostRoute` один раз увидел
    "маршрут через сам TUN", отверг его по инварианту выше и восстановил
    прежний шлюз; сама попытка восстановления один раз не удалась, пока
    физический адаптер был ещё выключен (`CreateIpForwardEntry2 failed`)
    — самоисцелилось на следующем срабатывании ~3 с спустя. Туннель
    доработал полный `--duration`, после `off` — 0 процессов, 0
    адаптеров, чистая таблица маршрутов.
  - **Известное ограничение (не решено)**: в узком окне между неудачной
    попыткой восстановить хост-маршрут и следующим срабатыванием
    `RecomputeHostRoute` (в тесте — около 3 с) анти-петлевого
    хост-маршрута до сервера нет вовсе, то есть трафик к серверу в этот
    момент теоретически может пойти через `/1`-маршруты в сам TUN.
    Для этапа 2 (нет kill-switch) это не проверялось отдельно; для
    этапа 3 (kill-switch должен по умолчанию резать трафик мимо TUN)
    это может быть уже не проблемой — стоит перепроверить после WFP.
  - Этап 2 по критериям приёмки (план, раздел 4) можно считать
    закрытым: on/off работает, переключение физического адаптера не
    рвёт туннель надолго, после `off` таблица маршрутов как до `on`.
- **Этап 3 (начат) — WFP kill-switch (план, 3.3), MVP-объём.**
  - `VlessTunnel.Native`: P/Invoke `fwpuclnt.dll` (`WfpEngine`,
    `WfpTypes`/`WfpWellKnown`) и класс `KillSwitch` — свой provider +
    sublayer (постоянные, вес 0xFFFF), на слоях ALE_AUTH_CONNECT V4/V6
    по 4 фильтра: разрешить xray.exe (по App ID), loopback, сам
    TUN-интерфейс (по LUID), запретить всё остальное. GUID'ы условий/
    слоёв сверены не по памяти, а по документации Microsoft и по
    исходникам wireguard-windows (боевой, широко проверенный WFP-kill-
    switch) — см. ниже, почему это оказалось не лишним.
  - Фиксированные GUID фильтров (не перечисление) — `Uninstall()`
    снимает ИМЕННО их по ключу, тот же принцип, что и `OwnRouteProtocol`
    для маршрутов (план: "снимаются по метке, не по памяти процесса").
  - **Четыре реальных бага найдены до и во время первого живого теста:**
    1. (юнит-тестом, до стенда) `FWP_DATA_TYPE` по памяti брался как
       `FWP_UINT8=0`, а на самом деле enum начинается с `FWP_EMPTY=0`,
       `FWP_UINT8=1` — тихо неверные условия фильтра без единой ошибки.
    2. (юнит-тестом) union `{UINT64 rawContext; GUID providerContextKey}`
       внутри `FWPM_FILTER0` по ошибке был смоделирован двумя подряд
       идущими полями вместо `FieldOffset`-объединения — сдвигало все
       смещения после него на 8 байт.
    3. (живым тестом) `FwpmProviderAdd0` падал с `FWP_E_NULL_DISPLAY_NAME`
       — `displayData.name` обязателен, был не проставлен.
    4. (живым тестом, самый серьёзный) **настоящий краш процесса**
       (`STATUS_ACCESS_VIOLATION` в KERNELBASE.dll, подтверждено дампом
       Windows Error Reporting) — `FWP_CONDITION_VALUE0.uint64` это
       `UINT64 *` (указатель на значение, "cannot be null" по
       документации), а не встроенное 8-байтное значение, как
       `uint8`/`uint16`/`uint32` в том же union'е. LUID TUN-интерфейса
       клался в поле напрямую, WFP пытался разыменовать его как адрес
       памяти. Исправлено выделением реальной памяти под значение.
       Место падения найдено пошаговым логом каждого вызова
       `FwpmFilterAdd0` (упал именно на условии по LUID) — без этого
       падение процесса без диагностики было неотличимо от зависания.
  - **Живой тест PASS на стенде** (через `with-rescue.sh`, снимок
    `clean`): легитимный трафik через туннель с включённым kill-switch
    проходит; `Stop-Process xray -Force` → интернет **пропадает**
    (проверено TCP-соединением к 1.1.1.1:443, до этого — работало);
    после штатного `off` — 0 фильтров/sublayer/provider (проверено
    `rescue.ps1`: `FWP_E_PROVIDER_NOT_FOUND`/`FWP_E_SUBLAYER_NOT_FOUND`,
    то есть уже снято самим `KillSwitch.Uninstall()`), таблица
    маршрутов чистая.
  - **Разрешение приватных сетей, DHCP и запрет DNS-утечек — добавлены и
    подтверждены живым тестом.** `KillSwitch.Install` получил `excludeLan`
    (по умолчанию `true`, соответствует `--proxy-lan`): 14 IPv4- +
    4 IPv6-фильтра permit по тому же списку сетей, что в
    `Core/ConfigBuilder.cs` (вес 5), плюс permit DHCP (порт 67/547, вес 5),
    плюс запрет порта 53 везде, кроме TUN (вес 8 — обязан перебивать
    permit приватных сетей, иначе DNS-сервер в LAN стал бы дырой).
    GUID'ы приватных фильтров не захардкожены 18 константами, а
    детерминированно выводятся из базового GUID подстановкой индекса —
    `Uninstall()` восстанавливает те же ключи без хранения списка.
  - Для этого понадобились ещё две WFP-структуры, `FWP_V4_ADDR_AND_MASK`
    и `FWP_V6_ADDR_AND_MASK` — и здесь нашлась третья по счёту тонкость
    документации: `FWP_V4_ADDR_AND_MASK` хранит адрес/маску в **host
    order** (не network order, как в SOCKADDR_INET) — в отличие от V6-
    варианта, который хранит адрес как обычный байтовый массив.
    Проверено юнит-тестами на конкретных CIDR (10.0.0.0/8 → 0x0A000000,
    192.168.1.0/24 → 0xC0A80100), не только на размер структуры.
  - **Живой тест PASS**: легитимный трафик через туннель по-прежнему
    работает; TCP на `<шлюз VM>:53` (там реально отвечает dnsmasq —
    проверено отдельно, без kill-switch, что подтверждает: блокировка
    настоящая, а не "там всё равно никто не слушал") — заблокирован,
    несмотря на permit приватных сетей; принудительное завершение
    xray.exe по-прежнему обрывает весь остальной интернет; после `off`
    — чистое состояние.
  - **`KillSwitch.Doctor()` и `VlessTunnel.Service.exe doctor` — сделаны
    и подтверждены живым тестом.** В отличие от `Uninstall()` (снятие по
    фиксированным GUID), `Doctor()` перечисляет ВСЕ фильтры в системе
    (`FwpmFilterEnum0`) и снимает те, чей provider/sublayer совпадает с
    нашим — находит зависшие фильтры, даже если список GUID в коде
    когда-нибудь разойдётся с тем, что реально стоит в системе. Тот же
    приём, что в черновике vm/rescue.ps1 (написан заранее, для случая
    "файлов программы уже нет вовсе"), но через `Marshal.PtrToStructure`
    по уже проверенному layout `FWPM_FILTER0`, а не ручное чтение смещений
    байт — rescue.ps1 намеренно остаётся самостоятельным и не зовёт
    этот метод.
  - **Живым тестом подтверждено и попутно перепроверено ключевое
    свойство**: WFP-фильтры с `FWPM_FILTER_FLAG_PERSISTENT` переживают
    резкое убийство процесса-службы (`Stop-Process -Force`, без
    штатного `off`) — трафик остаётся заблокированным; `doctor`
    находит и снимает все 30 фильтров (перечислением, не по памяти
    процесса) + sublayer + provider, связь восстанавливается. Первые
    попытки этого теста на "не сброшенной" виртуалке дали
    противоречивые результаты (тот же класс гонки таймингов, что уже
    встречался с маршрутами) — воспроизведено надёжно только на чистом
    снимке `clean` с увеличенной паузой перед проверкой связи после
    убийства процесса.
  - Этап 3 по критериям приёмки (план, раздел 4) можно считать
    закрытым: `taskkill /f xray.exe` → интернета нет, пока туннель не
    поднимется; утечек DNS нет (проверено точечно — TCP на DNS-порт
    LAN-шлюза заблокирован несмотря на permit приватных сетей; более
    широкая проверка dnsleaktest-подобным запросом с захватом трафика
    на физическом интерфейсе не проводилась, оставлена на будущее при
    необходимости); `doctor` снимает фильтры.
- **Этап 4 (начат) — IPC + CLI (план, 3.4/3.5).**
  - `VlessTunnel.Core/Ipc/IpcMessages.cs`: общий контракт протокола
    (NDJSON поверх именованного канала) — `IpcRequest`/`IpcResponse`/
    `IpcEvent`/`TunnelStatus`, в Core, потому что нужен обеим сторонам
    канала одинаково.
  - `VlessTunnel.Service`: `TunnelController` (состояние Off/Starting/
    On/Stopping/Error поверх `TunnelManager`, все операции под одним
    `SemaphoreSlim`, событие `StatusChanged`) и `IpcServer` — сервер
    `\\.\pipe\vless-tunnel` с ACL (SYSTEM, Administrators, опциональный
    `--allow-user`), несколько клиентов параллельно, подписка на события
    в рамках того же соединения (план: "трей не опрашивает службу по
    таймеру"). Новая подкоманда `serve` (временный отладочный вход, как
    `run`/`doctor` — полноценная установка службой ещё впереди, этап 6).
  - `VlessTunnel.Cli` (новый проект) — `vless-tunnel.exe`, реализованы
    `on`/`off`/`toggle`/`restart`/`status [--json]`/`set-link`/`doctor`;
    таймауты клиента (90с on/off/toggle/restart, 30с doctor, 10с
    остальное) больше худшего пути соответствующей команды на сервере
    (план: урок Linux про `toggle` с таймаутом 30с при пути `on` > 31с).
    `test`/`logs`/`autostart`/`update-core`/`ensure`/`uninstall`
    распознаются, но честно отвечают "ещё не реализовано" — это отдельные
    подсистемы (логи с ротацией, планировщик задач, скачивание релизов,
    установщик), не часть этого прохода.
  - **Найден и исправлен баг живым тестом**: в `IpcServer.CreatePipe()`
    результат `NTAccount.Translate(typeof(SecurityIdentifier))`
    приводился к типу `NTAccount` вместо `SecurityIdentifier` (опечатка
    в касте) — `InvalidCastException` при каждом запуске `serve` с
    `--allow-user`.
  - **Живым тестом подтверждено**: полный цикл `set-link → on → status
    → toggle → toggle → off` через IPC работает корректно (JSON
    туда-обратно, переходы состояний, `toggle` из состояния `Error`
    корректно пробует `on` заново); подписка на события (`subscribe`)
    доставляет весь путь `Off→Starting→On→Stopping→Off` проактивно, без
    опроса — то самое требование плана. По пути поймана и обойдена не
    связанная с продуктом хрупкость самой SSH-сессии стенда: несколько
    нативных `.exe` подряд через `& $cli ...` иногда роняют PowerShell с
    "доступ в отказе 0x5 на выходном пути консоли" (тот же класс, что
    раньше был с `Test-NetConnection`/`Start-Job`) — обходится
    `Start-Process -RedirectStandardOutput <файл>` вместо прямого
    захвата вывода.
  - **Не подтверждено живым тестом (честно, не выдаётся за готовое)**:
    "чужой пользователь не может управлять" — часть критерия приёмки
    этапа 4. Код ACL реализован по документации (явные правила только
    для SYSTEM/Administrators/`--allow-user`, без записи для `Everyone`
    — по умолчанию отказ), но проверить это живьём с двумя разными
    локальными пользователями не удалось: `Process.Start` с
    `LoadUserProfile=true` и явными учётными данными возвращает `null`
    даже для тривиального `cmd.exe` в этой SSH-сессии — похоже, сетевой
    логон не даёт нужных для этого привилегий, независимо от кода
    продукта. Нужен другой способ живой проверки (например, через
    интерактивную RDP/консольную сессию вместо SSH) — оставлено на
    будущее.
- **Этап 5 (начат) — трей и окно (план, 3.6).**
  - `VlessTunnel.Tray` (новый проект, WinForms — не WPF, для NotifyIcon
    + одного простого окна этого достаточно без лишней инфраструктуры).
    Иконки — те же SVG из `packaging/icons` Linux-версии, перегенерированы
    в многослойные `.ico` (16/24/32/48 px) через ImageMagick, поскольку
    у NotifyIcon нет нативной поддержки SVG.
  - `Core/Ipc/IpcClient.cs` — переиспользуемый клиент канала (запрос/ответ
    + подписка), вынесен из CLI в Core, чтобы трей не дублировал разбор
    NDJSON-протокола.
  - Меню трея: включить/выключить, статус, вставить ссылку, логи,
    автозапуск, выход — как в плане, плюс автозапуск (отдельным пунктом,
    план того явно не запрещает). Окно (`LinkForm`, собрано кодом, без
    визуального дизайнера): поле ссылки с маскирующим вводом (скрывает
    UUID полностью, не только его — проще и безопаснее, чем маскировать
    выборочно), статус, кнопка «Проверить» (честно отвечает "не
    реализовано" — `test`, этап 7), журнал событий IPC. Закрытие окна
    прячет его, не завершает трей — выход только через меню.
  - `AutostartManager` — ярлык в Startup через позднее связывание с
    `WScript.Shell` (COM), не через `<COMReference>` — обычная генерация
    COM-обёртки во время сборки не работает при кросс-компиляции с
    Linux-хоста (там нет COM), позднее связывание срабатывает только во
    время выполнения, на самой Windows-машине.
  - **Живая проверка на стенде (план требует именно этого — "иконка
    вкл/выкл/неизвестно" нельзя проверить иначе, кроме как увидеть
    глазами)**: трей запущен в РЕАЛЬНОЙ интерактивной сессии (автозапуск
    уже настроен на этом стенде, план 5.2) — не через SSH, у которого
    нет доступа к рабочему столу для NotifyIcon; для этого понадобился
    Планировщик заданий с флагом `/IT` (запуск в интерактивной сессии
    вошедшего пользователя). Дальше — `virsh screenshot` +
    QMP `input-send-event` с абсолютными координатами мыши (масштаб
    0–32767, не пиксели — экран 1024×768, `value = pixel / размер *
    32767`). Подтверждено скриншотами: значок появляется в области
    уведомлений (серый кружок `tray-unknown.ico` — трей ещё не успел
    получить состояние по IPC на момент проверки, что ожидаемо и само по
    себе корректное поведение), открывается по клику, контекстное меню
    показывает все шесть пунктов ровно как задумано.
  - Ещё не проверено живьём: смена цвета значка вкл/выкл вслед за
    реальным состоянием туннеля (сама подписка на события уже проверена
    в этапе 4 через `vless-tunnel watch` — здесь недостающее звено чисто
    визуальное); открытие и заполнение окна `LinkForm`; работа кнопки
    «Автозапуск» (создание/проверка ярлыка).
- **Этап 6 (начат, не проверено живьём) — служба Windows, установщик, CI (план, 3.7/3.8).**
  - `VlessTunnel.Service`: третий режим входа, `service` — тонкая обёртка
    (`VlessTunnelWindowsService : ServiceBase`) над тем же
    `TunnelController`/`IpcServer`, что и отладочный `serve`; лог идёт в
    Event Log (под SCM нет консоли). Именно это установщик регистрирует
    через `sc.exe create ... binPath="...VlessTunnel.Service.exe service"`.
  - `installer/vless-tunnel.iss` (Inno Setup) — один путь к бинарникам
    (план: урок Linux про дубль `/usr/bin`/`/usr/local/bin`), задачи
    автозапуска службы/трея, обновление поверх (остановить службу в
    `ssInstall` до копирования файлов → переустановить/перезапустить в
    `ssPostInstall`, конфиг в `%ProgramData%` не трогается), деинсталлятор
    — по данному в плане черновику (`[Code]`: вопрос "удалить настройки"
    с дефолтом "Нет", молчит при тихом удалении) плюс снятие службы
    (`sc stop`/`sc delete`) и WFP-фильтров (`VlessTunnel.Service.exe
    doctor` — ищет по provider/sublayer, а не по памяти процесса, значит
    сработает и после падения службы) и сертификата из `Root`/
    `TrustedPublisher` по отпечатку, если он задан.
  - `installer/trust.ps1` — разовый шаг доверия (план, 3.8, п.1): сверяет
    отпечаток `.cer` с зашитым в скрипт (сейчас пустой — сертификат ещё
    не выпущен, скрипт честно отказывается работать, а не пропускает
    проверку), ставит в `Root`+`TrustedPublisher`, `Unblock-File`,
    запускает установщик.
  - `.github/workflows/build.yml` — restore/test/publish (Service+Cli+Tray
    в одну `dist`, план: не дублировать рантайм), скачивание и проверка
    sha256 xray-core (сверено с `vm/pinned-versions.txt`), сборка
    установщика через Inno Setup (Chocolatey), подпись — если заданы
    секреты `SIGNING_PFX_BASE64`/`SIGNING_PFX_PASSWORD`/`CERT_THUMBPRINT`
    (иначе честное предупреждение и сборка без подписи, не тихий
    пропуск), релиз по тегу выкладывает `.exe` и `.sha256`.
  - **Не проверено живьём вообще (честно, не выдаётся за готовое)**:
    ни разу не запускался ни `ISCC.exe` (нет Inno Setup на Linux-хосте,
    где шла вся остальная сборка), ни сам workflow на GitHub, ни
    установка/обновление/удаление на стенде — критерии приёмки этапа 6
    (план, раздел 4: чистая установка, обновление поверх, удаление с
    «Да»/«Нет»/тихое, сертификат снимается) остаются открытыми до
    первого реального прогона. Также не выпущен сам сертификат
    (`$ExpectedThumbprint` в `trust.ps1` и `CertThumbprint` в CI пустые) —
    отдельный шаг для владельца, не то, что можно сделать за него.
- **Этап 7 (начат) — команда `test` (план, 3.5).**
  - `TunnelTester` (Service) + `TestResults` (Core/Ipc) + IPC-команда
    `test`, подключена в CLI (`vless-tunnel test`, убрана из списка
    "не реализовано") и в окно трея. Четыре проверки: HTTP через
    http-in (127.0.0.1:10809), SOCKS5 через socks-in (127.0.0.1:10808,
    у .NET `SocketsHttpHandler` нативная поддержка `socks5://` URI как
    прокси с .NET 6, отдельный SOCKS-клиент не понадобился), прозрачный
    TCP (прямое соединение с 1.1.1.1:443 — идёт через TUN, если туннель
    поднят), DNS (`Dns.GetHostAddressesAsync`, тоже идёт через TUN).
    UDP/QUIC сознательно не в этом проходе — нет способа проверить
    именно это без завязки на конкретный внешний сервис.
  - **Найдены и исправлены две реальные ошибки — не в тестируемом коде,
    а в собственной процедуре доставки на стенд**: при живом тесте
    `VlessTunnel.Cli` падал с `TypeLoadException` (забыл перезалить
    обновлённый `VlessTunnel.Core.dll`, откуда новый тип `TestResults`),
    а `VlessTunnel.Service` — с `FileNotFoundException` на
    `System.ServiceProcess.ServiceController.dll` (новая зависимость от
    режима `service`, тоже не была перезалита). И то, и другое — тот же
    урок, что уже был с `VlessTunnel.Service.dll` в этапе 3: заливать на
    стенд нужно ВСЕ dll из папки публикации, не только те, что явно
    поменялись по ощущению.
  - **Живой тест PASS**: до `on` — HTTP/SOCKS5 закономерно FAIL (прокси
    ещё не поднят), прозрачный TCP/DNS — OK (обычный прямой интернет);
    после `on` — все четыре проверки OK.
  - Вне этого прохода (план, этап 7 целиком шире одной команды):
    `doctor` с проверкой времени (рассинхронизация часов ломает
    REALITY/TLS), `update-core`/`self-update`, режим captive portal,
    логи с ротацией, аудит "секреты не попадают в вывод".
- Туннель на хосте и трафик виртуалки: проверено — `vless-tunnel.service` неактивен, петли в `virbr0` нет.

## Утренняя сессия — блокер спайка снят

Ночной блокер («трафик не доходит через TUN до Xray») решён утром —
оказался артефактом теста (проверка соединения раньше, чем Windows
применял только что добавленный маршрут), не реальной проблемой.
Подробности и правильный вывод для Service — в `docs/spike.md` и в
разделе «Статус» выше (этап 0б). Настоящая PQ-ссылка проверена и
работает через TUN end-to-end.
